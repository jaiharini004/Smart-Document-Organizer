apiVersion: v1
kind: Secret
metadata:
  name: sdo-secrets
type: Opaque
stringData:
  # Replace these with your actual keys before deploying to production
  SUPABASE_URL: "https://your-project.supabase.co"
  SUPABASE_KEY: "your-service-role-key"
  GROQ_API_KEY: "your-groq-api-key"
